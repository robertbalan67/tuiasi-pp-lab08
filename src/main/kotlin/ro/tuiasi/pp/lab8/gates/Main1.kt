package ro.tuiasi.pp.lab8.gates

import ro.tuiasi.pp.lab8.gates.builder.EightInputGateBuilder
import ro.tuiasi.pp.lab8.gates.builder.FourInputGateBuilder
import ro.tuiasi.pp.lab8.gates.builder.ThreeInputGateBuilder
import ro.tuiasi.pp.lab8.gates.builder.TwoInputGateBuilder

fun main() {
    // TODO: Construiește o poartă AND cu 2 intrări (true, false) și afișează ieșirea.
    //       Folosește TwoInputGateBuilder.

    // TODO: Construiește o poartă AND cu 3 intrări (true, true, true) și afișează ieșirea.

    // TODO: Construiește o poartă AND cu 4 intrări (true, true, false, true) și afișează ieșirea.

    // TODO: Construiește o poartă AND cu 8 intrări (toate true) și afișează ieșirea.

    // Exemplu de output așteptat:
    // AND(true, false)                        = false
    // AND(true, true, true)                   = true
    // AND(true, true, false, true)            = false
    // AND(true,true,true,true,true,true,true,true) = true
}
